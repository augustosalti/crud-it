export interface TornillosElement {
    Nombre: string;
    Precio: number;
    Formato: string;
    Marca: string;
    Acciones?: string;
  }

export interface OrdenTablas {
  orden: number;
  tipo: string;
}